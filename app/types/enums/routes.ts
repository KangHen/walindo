// types/enums/routes.ts
export enum Routes {
  AUTH = "auth",
  HOME = "/",
  DASHBOARD = "/dashboard",
  LOGIN = "/login",
  LOGOUT = "/logout",
  REGISTER = "/register",
  PROFILE = "/profile",
  WALLET = "/wallet",
  TRANSACTIONS = "/transactions",
  ME = "/me",
  USER = "/user",
  FAQS = "/faqs",
  PHONE = "/phone-number"
}
